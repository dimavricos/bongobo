


<div class="col-md-8 col-md-offset-2">
  <div class="row">

  	<h2 class="title">
      <div>{{removePavla this.params.station}} ({{models.length}} Playlists)</div> 
      <span></span>
    </h2>
    <div class="playlists-carousel"> 
      {{#grouped_each 9 models}}
      <div class="item">  
       {{#each this}}
       {{#noimage snippet.thumbnails.medium.url value="https://i.ytimg.com/vi/default.jpg"}}
       <a href="/tracks?station={{removeSpace ../../../params.station}}&name={{removeSpace snippet.title}}&playlistId={{id.playlistId}}" class="show">
        <div data-id="{{id.playlistId}}" class="image"> 
         <img src="{{snippet.thumbnails.medium.url}}" class="thumb" alt="{{snippet.thumbnails.medium.url}}"> 
         <div class="playlist-details"> 

          <i class="fa fa-play-circle"></i>
        </div> 

      </div>
      <h3 class="playlist-title">{{snippet.title}}</h3> 
    </a>  
    {{/noimage}}   

    {{/each}} 
  </div>
  {{/grouped_each}} 
</div>

</div>
</div>



{{#if models}}
{{else}}
<span class="no-results">NO PLAYLISTS FOUND</span>
{{/if}}



